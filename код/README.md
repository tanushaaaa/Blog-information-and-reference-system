# Информационно-справочная система блогов

## 🚀 БЫСТРЫЙ ЗАПУСК

### ШАГ 1: Создайте таблицы в базе данных

**Вариант 1: Используйте полный путь к psql**

```bash
cd "/Users/tanusha/Desktop/Курсовая/код"
/Library/PostgreSQL/14/bin/psql -U postgres -p 5434 -d blog_system < database.sql
```

**Вариант 2: Добавьте psql в PATH (один раз)**

Добавьте в файл `~/.bash_profile` или `~/.zshrc`:
```bash
export PATH="/Library/PostgreSQL/14/bin:$PATH"
```

Затем перезапустите терминал или выполните:
```bash
source ~/.bash_profile
```

После этого можно использовать просто `psql`.

**Введите пароль для `postgres`, когда попросит.**

**Проверка:**
```bash
/Library/PostgreSQL/14/bin/psql -U postgres -p 5434 -d blog_system
\dt
```

Должны быть видны таблицы: `users`, `categories`, `blogs`. Выйдите: `\q`

### ШАГ 2: Настройте пароль

Откройте `backend/src/main/resources/application.properties` и измените:
```properties
spring.datasource.password=ваш_пароль_от_postgres
```

### ШАГ 3: Запустите backend

```bash
cd "/Users/tanusha/Desktop/Курсовая/код/backend"
mvn spring-boot:run
```

**Дождитесь:** `Started BlogSystemApplication`

**ВАЖНО:** Не закрывайте этот терминал! Backend должен работать все время.

### ШАГ 4: Откройте frontend

Откройте файл `index.html` в браузере.

### ШАГ 5: Войдите в систему

- Email: `admin@example.com`
- Пароль: `admin123`
- Роль: `Администратор`

---

## 📋 ТРЕБОВАНИЯ

- Java 17+
- Maven 3.6+
- PostgreSQL 14+ (порт 5434)
- Браузер

---

## 🗄️ БАЗА ДАННЫХ

- Порт: **5434**
- База: `blog_system`
- Пользователь: `postgres`

---

## 📁 СТРУКТУРА

```
код/
├── index.html          # Frontend
├── style.css           # Стили
├── script.js           # JavaScript
├── database.sql        # SQL скрипт
├── backend/            # Spring Boot
│   ├── pom.xml
│   └── src/main/
│       ├── java/      # Java код
│       └── resources/
│           └── application.properties
└── README.md          # Эта инструкция
```

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Backend не запускается
- Проверьте Java: `java -version`
- Проверьте Maven: `mvn -version`
- Проверьте пароль в `application.properties`

### Ошибка "Failed to fetch"
- Убедитесь, что backend запущен
- Проверьте: `curl http://localhost:8080/api/categories`

### База данных не найдена
- Создайте: `/Library/PostgreSQL/14/bin/createdb -U postgres -p 5434 blog_system`
- Выполните скрипт: `/Library/PostgreSQL/14/bin/psql -U postgres -p 5434 -d blog_system < database.sql`

### Ошибка "psql: command not found"
- Используйте полный путь: `/Library/PostgreSQL/14/bin/psql`
- Или добавьте PostgreSQL в PATH (см. ШАГ 1)

---

## 📝 API ENDPOINTS

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/blogs` - Получить блоги
- `POST /api/blogs` - Создать блог
- `GET /api/categories` - Получить категории
- `GET /api/statistics` - Статистика

---

**Все готово к запуску!** Следуйте инструкциям выше.
